# ============================================================
# PAC Backend — Python Dependencies
# ============================================================

# FastAPI core
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database — async SQLAlchemy + PostgreSQL
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2
psycopg2-binary==2.9.9

# pgvector — 384-dim Crime DNA embeddings
pgvector==0.3.2

# PostGIS — geospatial crime analysis
geoalchemy2==0.15.2

# Pydantic v2 — request/response schemas
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# Security — JWT authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Graph database — criminal network intelligence
neo4j==5.22.0

# Cache + task queue
redis==5.0.7

# HTTP client — ML engine communication
httpx==0.27.0

# Utilities
python-dotenv==1.0.1

# Seed data generation
faker==25.9.1
numpy==1.26.4
